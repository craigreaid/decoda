import type { Story } from "@/lib/stories/types";

/**
 * Secular DecodaBooks story packs land here later.
 * Add JSON modules and export them from this array.
 */
export const booksStories: Story[] = [];
